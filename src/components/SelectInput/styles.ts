import styled from "styled-components";

export const Container = styled.div`
  > select {
    padding: 0.5rem 0.75rem;
    border: 0.25rem;
    outline: none;
  }

  &:first-child {
    margin-right: 1rem;
  }
`;
